# Allow installation of packages with different Node versions
engine-strict=false

# Use legacy peer dependencies resolution for better compatibility
legacy-peer-deps=true

# Automatically save installed packages
save=true
save-exact=false
